import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.mitchinson.haptic_test',
  appName: 'haptic-test',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
